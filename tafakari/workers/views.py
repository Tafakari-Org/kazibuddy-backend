from django.shortcuts import render

# Worker profile CRUD endpoints removed — any authenticated user can post/apply
# for jobs directly (see accounts.CustomUser). WorkerSkill has no CRUD endpoints
# exposed here today.